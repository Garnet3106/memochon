import 'package:loglu/components/shared/materials/chips/custom_chip.dart';

class HashTagChip extends CustomChip {
  const HashTagChip(
    super.label, {
    super.key,
    required super.isSelected,
    super.fillColor,
    super.selectedFillColor,
    super.border,
    super.selectedBorder,
    super.labelStyle,
    super.selectedLabelStyle,
  });
}
